{% for style in variables.site_styles %}
    <link rel="stylesheet" href="{{ style.href }}" {% if style.integrity %}integrity="{{ style.integrity }}" crossorigin="anonymous"{% endif %} />
{% endfor %}
